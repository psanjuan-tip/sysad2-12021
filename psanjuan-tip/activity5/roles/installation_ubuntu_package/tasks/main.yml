---
# tasks file for roles/installation_ubuntu_package 
  - name: Installing [Java]
    apt:
      name: default-jre
      state: latest
      update_cache: YES

  - name: Installing boto,ansible, and openstack
    pip:
      name:
        - boto
        - ansible
        - python-openstackclient
