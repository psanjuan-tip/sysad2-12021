---
# tasks file for roles/s_clone

- name: installing git on OpenSuse
  zypper:
    name: git
    state: latest

- name: cloning PE in opensuse
  git:
    repo: '{{ prelim_exam }}'
    dest: ./prelim-exam
    version: prelim-exam
