---
- name: vsftpd and firewall setup  
  hosts: ubuntu 
  roles: 
    - vsftpd_installation

- name: creating user and configuring the ssh 
  hosts: ubuntu
  roles:
    - user_ssh

- name: configuration of vsftpd and creating directories 
  hosts: ubuntu
  roles: 
    - Folders 
